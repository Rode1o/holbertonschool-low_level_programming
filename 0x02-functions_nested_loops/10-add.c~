#include "holberton.h"
/**
 *add - adds two integers and returns the result
  * @b: integer to be added
  * @a: integer to be added
  * Return: the result
**/
int add(int a, int b);
{
int c;
c = a + b;
return (c);
}
