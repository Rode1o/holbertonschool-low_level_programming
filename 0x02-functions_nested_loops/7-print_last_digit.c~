#include "holberton.h"
/**
 *print_last_digit - print the last digit about a number
 *
 *@n: number
 *Return: n last digit
 */
int print_last_digit(int n);
{
int o;
o = n % 10;
if (n < 0)
{
o = o * -1;
}
_putchar (o + 48);
return (o);
}
